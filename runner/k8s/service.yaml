apiVersion: v1
kind: Service
metadata:
  name: runner
  namespace: openclaw
  labels:
    app: runner
spec:
  type: ClusterIP
  selector:
    app: runner
  ports:
    - name: ssh
      protocol: TCP
      port: 22
      targetPort: 22
